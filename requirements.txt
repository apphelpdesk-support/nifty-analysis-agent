yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
streamlit>=1.36
plotly>=5.20
# optional: deep intraday backfill (2015+) via Zerodha Kite — install before
# running scripts/backfill_intraday.py; only needed if you skip the 60-day
# free yfinance collector
kiteconnect>=5.0
nselib>=2.5.1
